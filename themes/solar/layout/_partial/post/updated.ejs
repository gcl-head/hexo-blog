<% if (post.updated) { %>
    <div class="<%= class_name %>">
        <time datetime="<%= date_xml(post.updated) %>" itemprop="datePublished"><%= date(post.updated, config.date_format) %></time>
    </div>
<% } %>
